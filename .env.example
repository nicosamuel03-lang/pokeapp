# Copier vers .env.local et renseigner les valeurs.
# Clerk — clés : https://dashboard.clerk.com → API Keys

# Backend Express (dev : http://localhost:4000, prod : URL Railway). Surcharge les .env.development / .env.production si besoin.
# VITE_API_URL=http://localhost:4000

# eBay Browse API (OAuth2 côté serveur uniquement — pas de préfixe VITE_)
# https://developer.ebay.com/my/keys
EBAY_CLIENT_ID=
EBAY_CLIENT_SECRET=

# eBay Finding API (findCompletedItems) — App ID, Cert ID, Dev ID
EBAY_APP_ID=
EBAY_CERT_ID=
EBAY_DEV_ID=

VITE_CLERK_PUBLISHABLE_KEY=

# URLs d’auth (SPA) — doivent correspondre aux routes React (`App.tsx`)
VITE_CLERK_SIGN_IN_URL=/sign-in
VITE_CLERK_SIGN_UP_URL=/sign-up
VITE_CLERK_AFTER_SIGN_IN_URL=/
VITE_CLERK_AFTER_SIGN_UP_URL=/

# eBay / prix marché : MOCK = prix figés (voir src/services/ebayMarketPrice.ts), sans API
# VITE_EBAY_STATUS=MOCK

# Précharger toutes les images produit au démarrage (déconseillé en prod : gros volume sortant)
# VITE_PRELOAD_PRODUCT_IMAGES=true
